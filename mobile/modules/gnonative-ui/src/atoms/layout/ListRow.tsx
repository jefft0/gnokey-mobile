import styled from 'styled-components/native'

/**
 * A row component for horizontal list items, styled as a touchable opacity.
 */
export const ListRow = styled.TouchableOpacity`
  width: 100%;
  flex-direction: row;
  min-height: 48px;
  align-items: center;
  justify-content: space-between;
`
